// Name: Owen Monus
// Student Number: 200482797
// Date: Oct 2nd, 2022

/* Filename: main.cpp
   Purpose: To convert the structure into a class implementation. */

#include "Cat.h"
#include <iostream>
using namespace std;

int main
) {  
	Cat myCat;
  
	string colours[5];  
	colours[0] = "white";  
	colours[1] = "";  
	Cat averageC at(44, 24, 28, "blue", "medium", colours);
  
	myCat.readCat();
  
	cout << "The average cat's first colour is " << averageCat.furColours[
		 << endl;  
	cout << "------------------------------------------------------" << endl  
	cout << "This is myCat:" << endl  
	print Cat(myCat)  
	if (isTal ler(myCat, averageCa
 t)) {  
		cout << "My cat is taller than the average cat\n"  
 
	} e
 lse {  
		cout << "My cat is shorter than the average cat\n"  
	  
	if (isCal ico(myCa
 t)) {  
		cout << "My cat is a calico\n"  
	  }  
	return 0;
}
